이 문서는 Carelog 프로젝트의 단계별 개발 기능과 요구사항을 정의합니다. ERD는 `Lean MVP` → `상용 버전` → `최종 버전`으로 점진적으로 확장되는 것을 전제로 합니다.

---

# **1. 전체 기술 아키텍처 및 공통 전략**

## **2.1. 애플리케이션 아키텍처 (Application Architecture)**

**'도메인 중심의 계층형 아키텍처 (Domain-Centric Layered Architecture)'** 를 채택합니다. 이 아키텍처는 복잡한 비즈니스 로직을 체계적으로 관리하고, 서비스의 확장성과 유지보수성을 극대화하기 위한 최선의 선택입니다.

- **핵심 사상**: 비즈니스 규칙과 로직(도메인)을 애플리케이션의 심장부(Core)에 집중시키고, 외부 기술(UI, DB, 메시징 등)로부터 보호하여 안정적이고 유연한 구조를 만듭니다.
- **기대 효과**: 거대 서비스 클래스(God Class) 문제를 방지하고, 코드의 응집도를 높이며, 향후 마이크로서비스로의 전환까지 고려한 확장성 있는 설계를 보장합니다.

**주요 계층(Layers):**

1.  **Presentation Layer (Controller)**
    - **역할:** API 엔드포인트. HTTP 요청/응답 처리, 데이터 검증(DTO), 인증/인가 확인.
    - **구현:** `@RestController`

2.  **Application Layer (Service)**
    - **역할:** 사용자 유스케이스(Use Case) 처리. 트랜잭션 관리, 도메인 객체 및 외부 서비스 흐름 조율(Orchestration).
    - **구현:** `@Service`, `@Transactional`

3.  **Domain Layer (Entity, Domain Service)**
    - **역할:** 애플리케이션의 핵심. 순수한 비즈니스 로직과 규칙의 집합. 도메인 객체(Entity)가 스스로 상태와 행위를 가집니다.
    - **구현:** `@Entity`, POJOs (Plain Old Java Objects)

4.  **Infrastructure Layer (Repository, External Clients)**
    - **역할:** 외부 세계와의 통신. DB 연동, 파일 스토리지, 메시징 큐, 외부 API 호출 등 기술적인 세부사항 담당.
    - **구현:** Spring Data JPA `@Repository`, Feign/WebClient

---

## **2.2. 기술 구조 핵심 요약 (Core Technology Stack)**

| 영역 (Domain) | 주요 기술/방법 (Technology/Method) |
| --- | --- |
| **인증/권한** | Spring Security, JWT (JWT Filter ↔ SecurityContextHolder 분리 구조 적용) |
| **ORM/DB** | Spring Data JPA, PostgreSQL, QueryDSL |
| **암호화** | Jasypt 또는 AES (필드 레벨 암호화) |
| **이벤트 처리** | Spring Events + `@Async` |
| **알림** | FCM, Twilio, Email 등 |
| **AI 연동** | LangChain, FAISS (또는 PGVector), LangGraph |
| **채팅** | Node.js, Redis (Pub/Sub) |
| **파일 저장** | AWS S3 (서명 URL(Presigned-URL) 사용 고려) |
| **배포** | Docker, AWS EC2, GitHub Actions (CI/CD) |
| **문서화** | Swagger (Springdoc) |
| **모니터링** | Prometheus + Grafana (임계치 기반 알림 조건 설정, 예: 예약 폭주 시 알림) |
| **비동기 트랜잭션** | `@TransactionalEventListener(fallbackExecution = true)` |
| **테스트** | `@DataJpaTest`, `@WebMvcTest` 등 계층별 슬라이스 테스트 권장 |

## **2.2. 추가 고려 사항 (Best Practices for Stability)**

- **감사 로그 테이블 (`audit_logs`):** 모든 주요 데이터의 변경 이력을 추적하여 문제 발생 시 원인 파악 및 데이터 복구를 용이하게 합니다.
- **중복 예약 방지:** DB Unique 제약 조건과 비관적 락(`Pessimistic Lock`)을 함께 사용하여 동시성 이슈를 원천적으로 차단합니다.
- **알림 발송 이력:** `notifications` 테이블에 발송 성공/실패 여부를 기록하고, 실패 시 재시도 로직을 구현합니다.
- **서명 URL (Presigned-URL):** 파일에 직접 접근하는 URL 대신, 권한과 유효시간이 부여된 임시 URL을 발급하여 보안을 강화합니다.
- **GPT 응답 메타 정보 저장:** `prompt_histories` 테이블에 응답 시간, 사용 모델, 비용 관련 토큰 수 등 메타 정보를 저장하여 디버깅 및 비용 분석에 활용합니다.
- **Rate Limiter:** Redis 등을 이용하여 악의적인 대량 요청으로부터 서버를 보호합니다.

## **2.3. 고급 아키텍처 패턴 (Advanced Architectural Patterns)**

### **🧠 OpenPolicyAgent (OPA) - 유연한 정책 관리**

- **설명:**
    - 역할, 리소스, 시간, 조건 등 복합적인 정책을 코드가 아닌 외부 정책 엔진에서 중앙 관리합니다.
- **적용 예시:**
    - “치료사는 자신이 담당하는 고객의 일지만 열람 가능”
    - “운영자는 모든 예약을 수정할 수 있지만, 과거의 예약은 수정 불가”
- **추천 적용 위치:**
    - API Gateway 또는 Spring Security Filter 레이어에서 OPA 엔진에 REST API로 질의하여 인가(Authorization)를 결정합니다. 이를 통해 RBAC의 한계를 넘는 매우 동적이고 세분화된 접근 제어가 가능해집니다.

### **🔄 Redis Stream - 안정적인 메시지 큐**

- **설명:**
    - 기존 Redis Pub/Sub은 메시지가 유실될 수 있는 반면, Redis Stream은 Kafka와 같이 데이터를 보존하는 메시지 큐입니다. 메시지 재처리 및 지연 작업에 적합합니다.
- **추천 적용 위치:**
    - **알림 시스템:** 알림 발송 실패 시, 재시도 큐(Retry Queue)에 넣어 순차적으로 재처리합니다.
    - **채팅 시스템:** 메시지를 안정적으로 저장하고 순서를 보장하며, 특정 시점부터의 메시지를 다시 불러오는 데 사용합니다.
    - **예약 시스템:** 예약 확정 같은 중요한 트랜잭션을 지연 처리(Deferred Processing)하는 배치 작업에 활용합니다.

### **📊 Spring Batch - 대용량 일괄 처리**

- **설명:**
    - 대용량 데이터의 정기적인 처리, 통계 생성, 마이그레이션 작업에 최적화된 프레임워크입니다.
- **적용 예시:**
    - 매일 자정, 전날의 상담 일지 데이터를 바탕으로 통계 리포트를 생성.
    - 서비스 정책에 따라 1년이 지난 예약 데이터를 별도의 아카이브 테이블로 이관.
- **추천 적용 위치:**
    - `@Scheduled` 어노테이션으로 주기적인 배치를 트리거하고, `JobLauncher`를 통해 정의된 `Job`과 `Step`을 실행하는 비동기 일괄 처리 흐름에 적용합니다.

## **2.4. V1 개발 전략 및 아키텍처 진화 로드맵**

Carelog 프로젝트는 V1에서 개발 속도를 우선하고, V2 이후 단계에서 아키텍처를 점진적으로 고도화하는 전략을 채택합니다. 이를 위해 V1에서는 Spring Boot의 일반적인 계층형 아키텍처를 따르되, 향후 헥사고날 아키텍처로의 전환을 고려한 설계 원칙을 적용합니다.

### **V1 최소 세트 (초기 개발 권장 아키텍처)**

이 조합은 개발 속도가 빠르고 운영이 안정적이며, Spring 개발에 가장 익숙한 형태입니다.

*   **Controller**: 웹 경계
    *   요청 DTO 바인딩 + 검증 (`@Valid`) → Service 호출 → 응답 DTO로 반환
    *   엔티티를 직접 반환하지 말기 (민감정보/지연로딩 이슈 방지)
*   **Service**: 유스케이스 (`@Transactional`)
    *   권한/검증/흐름 조립 + 비밀번호 해시/암복호화 + Repository 호출
    *   비즈니스 규칙은 가능하면 엔티티/VO 메서드로 내리기
*   **Repository**: Spring Data JPA
    *   `extends JpaRepository<엔티티, ID>`로 바로 사용 (가장 빠름)
    *   조회 전용은 프로젝션/DTO 쿼리로 N+1 줄이기
*   **Entity**: JPA 겸용 도메인
    *   `@Data` 금지 → `@Getter` + 필요한 곳만 `@Setter`
    *   정적 팩토리/메서드로 불변식 체크 (예: `Reservation.create(...)`)
    *   시간은 `@CreatedDate`/`@LastModifiedDate`, 민감정보는 `AttributeConverter`로 암/복호화
*   **DTO**: “웹 경계”에만
    *   요청/응답 DTO 따로 (엔티티 노출 금지)
    *   변환은 컨트롤러 or `UserResponse.from(entity)` 같은 정적 메서드로 간단 처리 (별도 매퍼 불필요)

### **개념별 “도입 기준(트리거)”**

아래 트리거가 2개 이상 생기기 전까지는 해당 개념을 도입하지 않아도 됩니다.

1.  **UseCase (서비스 분리/명시적 유스케이스)**
    *   **안 써도 되는 경우:** 서비스 클래스가 유스케이스 1~2개만 담당, 복잡도 낮음
    *   **도입 트리거:**
        *   하나의 서비스가 여러 흐름(생성/변경/취소/승인…)을 담아 커짐
        *   권한/검증/트랜잭션 경계를 유스케이스별로 명확히 나누고 싶다
    *   **도입 방법:** 기존 `UserService`를 `SignUpUserService` 같은 유스케이스 단일 책임 클래스로 쪼개고, 이름만 `UseCase`로 바꿔도 충분.

2.  **Mapper (변환기)**
    *   **안 써도 되는 경우:** 도메인=JPA 겸용, 웹 DTO 변환이 단순 → `UserResponse.from(entity)` 정적 메서드로 처리
    *   **도입 트리거:**
        *   도메인 POJO ↔ JPA 엔티티 분리가 필요해짐 (지연로딩/규칙 충돌, 저장소 교체, CQRS)
        *   웹 DTO가 커져서 변환 코드가 여기저기 중복
    *   **도입 방법:**
        *   **Web Mapper:** `Request↔Command`, `Entity↔Response` 변환 모으기 (수동/MapStruct)
        *   **Persistence Mapper:** `Domain↔JPA 엔티티` 변환 (분리 시점에만)

3.  **VO (Value Object)**
    *   **안 써도 되는 경우:** `email`, `phone` 같은 값 검증이 단발/단순
    *   **도입 트리거:**
        *   같은 값 규칙을 여러 곳에서 재사용, 테스트 필요
        *   강한 형식/불변이 중요 (통화/금액/기간/이메일 등)
    *   **도입 방법:** `record Email(String value) { /* 검증 */ }` 처럼 가볍게. JPA 컬럼이면 `@Embeddable`로.

4.  **포트/어댑터 (헥사고날)**
    *   **안 써도 되는 경우:** DB는 JPA 그대로, 외부 연동이 1~2개이고 교체 가능성 낮음
    *   **도입 트리거:**
        *   알림/결제/AI/파일 등 외부 연동이 늘고 공급자 교체 가능성↑
        *   저장소 이원화 (RDB+벡터/S3) 또는 조회 복잡도 증가
    *   **도입 방법:** 지금 쓰는 `*Client` 인터페이스들을 `Port`로 이름만 바꾸고, 구현을 `Adapter` 패키지로 이동.

5.  **도메인 이벤트/아웃박스**
    *   **안 써도 되는 경우:** 동기 트랜잭션 내에서 후처리 가능, 규모 작음
    *   **도입 트리거:**
        *   예약/결제/알림 등 후처리가 많고 비동기화/재시도 필요
        *   마이크로서비스 분리 검토
    *   **도입 방법:** 처음엔 `@TransactionalEventListener(AFTER_COMMIT)`로 시작 → 필요 시 Outbox 테이블 도입.

### **V2 Spring Cloud Gateway 도입 시 변경 선택사항**

V2에서 Spring Cloud Gateway를 도입할 경우, 이는 시스템의 외부 아키텍처 변화에 해당합니다. 내부 서비스의 아키텍처가 어떻게 설계되었는지에 따라 Gateway 도입의 용이성이 달라집니다.

*   **현재 V1 아키텍처 (JPA 겸용 도메인 엔티티):**
    *   Gateway는 주로 라우팅, 인증/인가, 로깅 등 횡단 관심사를 처리합니다.
    *   내부 서비스는 Gateway의 존재를 직접적으로 알 필요는 없지만, Gateway가 처리하는 인증/인가 방식(예: JWT 토큰 검증)에 맞춰 `Presentation Layer`의 필터나 인터셉터 로직을 조정해야 할 수 있습니다.
    *   만약 V2에서 MSA로 전환하며 Gateway를 도입한다면, 각 서비스의 분리 작업과 함께 Gateway 연동을 고려해야 합니다.

*   **헥사고날 아키텍처로 전환된 경우:**
    *   Gateway 도입은 `Presentation Layer`의 **인바운드 어댑터**가 변경되는 시나리오로 볼 수 있습니다.
    *   `Application Layer`와 `Domain Layer`는 Gateway의 도입 여부와 상관없이 비즈니스 로직에 집중할 수 있습니다.
    *   Gateway가 처리하는 인증/인가 방식에 맞춰 `Presentation Layer`의 어댑터만 변경하면 되므로, 핵심 비즈니스 로직에 미치는 영향이 최소화됩니다.
    *   이는 MSA로의 전환 시 각 마이크로서비스의 내부 구조가 견고하여 Gateway와의 연동이 더욱 유연해지는 장점으로 이어집니다.

---

# **2. 단계별 기능 명세서**

### **✅ 1단계: Lean MVP (v1.0) - 핵심 가치 검증**

- **목표:** 서비스의 가장 본질적인 기능(관계, 예약, 기록)을 구현하여 빠르게 시장의 핵심 가설을 검증합니다.
- **참고 ERD:** `ERD_v1.md`

### **1.1. 회원 인증/권한 관리 (기본)**

- **기능 설명:** 사용자는 서비스에 가입하고 로그인할 수 있으며, 치료사 또는 고객의 기본 역할을 부여받습니다.
- **주요 요구사항:**
    - BCrypt 해싱을 사용한 안전한 비밀번호 저장.
    - Jasypt 등을 활용하여 `phone_encrypted`, `address_encrypted` 등 민감 정보 필드 암호화.
    - JWT 기반의 인증 토큰(Access/Refresh) 발급 및 검증을 통한 세션 관리.
    - 가입 시 'THERAPIST', 'CLIENT' 등 기본적인 역할 지정.
    - 개인정보 처리 등 필수적인 사용자 동의 여부 기록.
- **관련 ERD 테이블:** `users`, `tokens`, `consent_records`

### **1.2. 고객-담당자 관계 설정**

- **기능 설명:** 치료사와 고객 간의 공식적인 관계를 시스템에 설정하여 데이터 접근의 기반을 마련합니다.
- **주요 요구사항:**
    - 특정 치료사와 특정 고객을 1:1로 연결.
    - 연결된 관계의 상태('활성', '종료' 등)를 관리.
- **관련 ERD 테이블:** `relations`, `users`

### **1.3. 예약 시스템**

- **기능 설명:** 고객은 담당 치료사와의 상담/치료 일정을 예약하고 관리할 수 있습니다.

- **주요 요구사항:**
    - 특정 관계 내에서 예약 일정 생성, 수정, 취소.
    - 동일한 시간에 중복 예약이 생성되지 않도록 방지.
- **관련 ERD 테이블:** `reservations`, `relations`
- **기술 제안:** DB의 Unique 제약 조건(`reservations` 테이블의 `(relation_id, start_time)`)과 서비스 레벨의 유효성 검증을 병행하여 이중 예약을 방지합니다.

### **1.4. 상담·치료 일지 작성**

- **기능 설명:** 치료사는 템플릿을 활용하여 고객과의 상담 내용을 체계적으로 기록합니다.
- **주요 요구사항:**
    - 관리자가 사전에 정의한 템플릿 목록 제공.
    - 템플릿을 선택하여 JSON 형식으로 내용 작성 및 저장.
    - 작성된 일지는 특정 관계 및 작성자와 연결되어야 함.
- **관련 ERD 테이블:** `relation_entries`, `entry_templates`, `relations`

---

### **✅ 2단계: 상용 버전 (v2.0) - 수익 모델 구축 및 전문성 강화**

- **목표:** MVP 위에 사용자가 비용을 지불할 만한 전문 기능과 편의 기능을 추가하여 완성도 높은 상용 서비스를 구축합니다.

### **2.1. 권한 관리 시스템 고도화 (RBAC)**

- **기능 설명:** 역할을 테이블 기반으로 체계적으로 관리하여 향후 다양한 역할(예: '관리자', '수련생') 추가에 유연하게 대응합니다.
- **주요 요구사항:**
    - 역할 정보(이름, 도메인, 설명)를 DB에서 관리.
    - 사용자와 관계의 역할 정보를 ID 참조 방식으로 변경.
- **관련 ERD 테이블:** `roles` (신규), `users` (변경), `relations` (변경).

### **2.2. 고객 정보 관리 확장**

- **기능 설명:** 사용자에 대한 더 상세하고 구조화된 정보를 수집하고 관리합니다.
- **주요 요구사항:**
    - 사용자의 직종, 생년월일 등 추가 정보 필드 구현.
    - 특정 직종에만 필요한 정보(예: 치료사의 자격번호)를 JSON 형식으로 유연하게 저장.
- **관련 ERD 테이블:** `users` (컬럼 추가).

### **2.3. 진행 단계(Lifecycle) 관리**

- **기능 설명:** 고객 관계의 상태가 변경될 때마다 그 이력을 자동으로 기록하여 전체 과정을 추적할 수 있도록 합니다.
- **주요 요구사항:**
    - 관계 상태가 '상담 중'에서 '종결'로 변경될 때, 변경 시간, 주체, 사유 등을 기록.
- **관련 ERD 테이블:** `relation_status_history` (신규), `relations`.
- **기술 제안:** Spring Events와 `@EventListener`를 사용하여 상태 변경 로직과 이력 저장 로직을 분리합니다.

### **2.4. 파일 업로드 기능**

- **기능 설명:** 상담에 필요한 각종 문서나 이미지를 안전하게 업로드하고 관리합니다.
- **주요 요구사항:**
    - 파일 업로드 및 S3 등 외부 스토리지에 저장.
    - 파일 메타데이터(파일명, 경로, 소유자 등)를 DB에 저장.
- **관련 ERD 테이블:** `file_metadata` (신규).

### **2.5. 알림 시스템**

- **기능 설명:** 예약, 일지 작성 등 주요 이벤트 발생 시 사용자에게 SMS, 이메일, 푸시 등 다양한 채널로 알림을 보냅니다.
- **주요 요구사항:**
    - 알림 발송 및 성공/실패 이력 저장.
    - 예약 변경/취소 시 관련자에게 자동 알림.
- **관련 ERD 테이블:** `notifications` (신규).
- **기술 제안:** Spring `ApplicationEventPublisher`와 `@Async`를 활용하여 비동기 방식으로 알림을 처리합니다.

### **2.6. 구독/결제 시스템**

- **기능 설명:** 서비스의 핵심 수익 모델로, 사용자가 유료 플랜을 구독하고 결제할 수 있는 기능을 제공합니다.
- **주요 요구사항:**
    - 사용자별 구독 플랜 정보 관리.
    - 결제 상태(활성, 취소, 실패) 추적.
- **관련 ERD 테이블:** `user_subscriptions` (신규).

---

### **✅ 3단계: 고도화 버전 (v3.0) - 서비스 차별화 및 경험 극대화**

- **목표:** AI, 실시간 통신 등 기술적 차별점을 도입하고, 전문가용 심화 기능을 통해 시장을 선도하는 플랫폼으로 발전시킵니다.

### **3.1. AI 치료 플래너 추천**

- **기능 설명:** 축적된 상담 일지 데이터를 기반으로 AI가 치료사에게 유용한 인사이트나 계획을 추천합니다.
- **주요 요구사항:**
    - LangChain RAG 구조를 활용한 상담 일지 벡터화 및 유사 기록 검색.
    - GPT 등 LLM과의 연동 및 응답 이력, 사용자 피드백 저장.
- **관련 ERD 테이블:** `prompt_histories`, `prompt_sources`, `vector_chunks`, `langchain_traces` (모두 신규).

### **3.2. 실시간 채팅**

- **기능 설명:** 치료사와 고객 간의 원활한 소통을 위한 실시간 채팅 기능을 제공합니다.
- **주요 요구사항:**
    - WebSocket 기반 실시간 메시지 송수신.
    - 오프라인 메시지 저장 및 푸시 알림 연동.
    - 메시지 읽음 상태 처리.
- **관련 ERD 테이블:** `chat_sessions`, `chat_messages` (모두 신규).
- **기술 제안:** Node.js, Socket.IO, Redis Pub/Sub을 활용하여 채팅 서버를 구축합니다.

### **3.3. 전문가용 심화 기능**

- **기능 설명:** 치료의 질을 높이기 위한 목표 설정 및 표준화된 평가 기능을 도입합니다.
- **주요 요구사항:**
    - 치료 목표 설정 및 진행 상황 추적.
    - PHQ-9, GAD-7 등 표준화된 평가 도구 제공 및 결과 기록.
- **관련 ERD 테이블:** `goals`, `goal_progress`, `assessments`, `assessment_results` (모두 신규).

### **3.4. 기타 고도화 기능**

- **기본 통계 대시보드:** QueryDSL 등을 활용하여 주요 운영 지표를 시각화하고 CSV/Excel로 내보냅니다.
- **캘린더 외부 연동:** 예약 시스템을 구글 캘린더 등 외부 서비스와 연동합니다.
- **운영 모니터링:** Actuator, Prometheus 등을 도입하여 시스템 상태를 모니터링합니다.

---

### **3. 개발자 정보 (Role & Responsibility)**

이 프로젝트의 **기술 리더(Tech Lead)이자 핵심 개발자**로서, 다음과 같은 경험과 책임을 기반으로 프로젝트를 주도합니다.

- **주요 경험 및 역량:**
    - NestJS, FastAPI 기반의 백엔드 시스템 설계 및 개발 경험.
    - 의존성 주입(DI), 도메인 주도 설계(DDD) 등 모던 애플리케이션 아키텍처 구현 역량.
    - 김영한님 강의를 통해 Spring의 핵심 원리 및 동작 방식에 대한 깊은 이해를 갖추고 있음.

- **본 프로젝트에서의 핵심 역할:**
    - 전체 시스템의 아키텍처 설계를 주도하고, 도메인 중심의 계층형 구조가 일관성 있게 유지되도록 관리.
    - 20개 이상의 테이블로 확장될 것을 고려하여, 서비스의 확장성과 유지보수성을 보장하는 기술적 의사결정 책임.
    - 트랜잭션 전파, 계층 간 의존성 관리, 도메인 이벤트, 비동기/배치 처리 등 Spring Boot의 실무적인 주제에 대해 깊이 고민하고 적용하여 아키텍처의 안정성을 극대화.

### **4. Gemini 협업 모델 (Collaboration Model)**

본 프로젝트에서 Gemini는 Tech Lead의 생산성을 극대화하는 **AI 페어 프로그래머(AI Pair Programmer)** 역할을 수행합니다.

- **1단계: 스캐폴딩 (Scaffolding):**
    - Gemini는 정의된 아키텍처에 따라 초기 디렉터리 구조, 설정 파일 등 개발에 필요한 뼈대를 생성합니다.
    - Tech Lead의 지시에 따라 Entity, DTO, Repository 등 반복적인 보일러플레이트 코드를 생성할 수 있습니다.

- **2단계: 핵심 로직 구현 (Core Logic Implementation):**
    - **Tech Lead가 직접 핵심 비즈니스 로직을 작성**하며, 시스템의 가장 중요한 부분을 책임지고 개발합니다.

- **3단계: 리팩토링 및 테스트 지원 (Refactoring & Test Support):**
    - Tech Lead의 요청에 따라 코드 리팩토링, 테스트 코드 초안 작성, 성능 개선 방안 제안 등의 보조적인 역할을 수행합니다.

- **기본 원칙:** Gemini는 Tech Lead가 복잡한 비즈니스 로직과 아키텍처 설계에 집중할 수 있도록, 반복적인 작업을 자동화하고 기술적인 대안을 제시하는 역할을 합니다.

- **커밋 제안 (Proactive Committing):**
    - Gemini는 논리적인 작업 단위가 완료되었다고 판단되면, 변경 사항에 대한 요약과 커밋 메시지 초안을 작성하여 Tech Lead에게 먼저 제안합니다. 최종 커밋 결정과 푸시(Push)는 Tech Lead가 직접 수행합니다.